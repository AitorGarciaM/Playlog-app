export default function LogOutIcon(props: {width: number; height: number; className?: string}) {
    return <img src="src\assets\icons\Log out.svg" alt="Log Out Icon" width={props.width} height={props.height} className={props.className} />;
}